{
  "name": "velvacloud-daemon",
  "version": "0.1.0",
  "private": true,
  "description": "Node agent for provisioning and managing containers (mTLS, Docker)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dockerode": "^4.0.4",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.12",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.3"
  }
}