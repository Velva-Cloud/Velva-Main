# Development env (Next.js)
# Proxy /api to your local backend
API_PROXY_TARGET=http://localhost:4000
# Use relative /api for requests in the browser (rewritten by Next to API_PROXY_TARGET)
NEXT_PUBLIC_API_BASE_URL=/api

# Public daemon image to run on nodes (override if you publish under a different path)
NEXT_PUBLIC_AGENT_IMAGE=ghcr.io/velva-cloud/velva-daemon:latest

# PayPal client id to render JS buttons
NEXT_PUBLIC_PAYPAL_CLIENT_ID=