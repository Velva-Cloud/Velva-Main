# Production env (Next.js)
# Normally your reverse proxy routes /api to backend, so just use relative base
NEXT_PUBLIC_API_BASE_URL=/api
# No API_PROXY_TARGET in production; your proxy/CDN should handle routing